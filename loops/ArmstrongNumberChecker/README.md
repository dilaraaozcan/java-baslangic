# 🔢 Armstrong Number Checker — Java Application

This project checks whether a number entered by the user is an **Armstrong number**.  
An Armstrong number is a special number in which the sum of each digit raised to the power of the total number of digits is equal to the number itself.

---

## ✨ Features

- Takes a number from the user  
- Determines the number of digits  
- Calculates the power of each digit  
- Sums the calculated values and performs the Armstrong check  
- Prints the result to the console  

---

## 🧠 Structures Used

- User input with `Scanner`  
- Digit count using `String.valueOf().length()`  
- Iteration with a `while` loop  
- Digit extraction using `%` and `/` operators  
- Power calculation with `Math.pow()`  
- Comparison using `if–else` statements  

---

## 📁 Files

- Main.java  
- README.md  

---

## 📝 Purpose

To reinforce loop logic, mathematical operations, and algorithmic thinking in Java.

